language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-weblaunch' ]; then cd .. && eval "mv $currentfolder generator-weblaunch" && cd generator-weblaunch; fi

